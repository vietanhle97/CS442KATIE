<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="200dp">
    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"
        android:id="@+id/camera_holder"
        app:cardCornerRadius="35dp">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_cancel"
            android:elevation="5dp"
            android:id="@+id/unverified" />
    </androidx.cardview.widget.CardView>
    <TextView
        android:layout_width="match_parent"
        android:id="@+id/attendance_checked_notification"
        android:layout_height="wrap_content"
        android:layout_below="@id/camera_holder"
        android:layout_marginBottom="20dp"
        android:textSize="18dp"
        android:text="Your attendance is checked\n stay in class to maintain attendance"
        android:gravity="center"
        />
</RelativeLayout>